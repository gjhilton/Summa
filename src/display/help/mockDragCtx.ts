import type { DragCtxValue } from '@/display/DragCtx'

export const MOCK_DRAG_CTX: DragCtxValue = {
  listeners: undefined,
  attributes: {
    role: 'button',
    tabIndex: 0,
    'aria-disabled': false,
    'aria-pressed': undefined,
    'aria-roledescription': 'sortable',
    'aria-describedby': '',
  },
}
