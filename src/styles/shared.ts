/**
 * Shared focus ring applied to all interactive elements (buttons, inputs, toggles).
 */
export const focusRing = {
	outlineWidth: 'medium',
	outlineStyle: 'solid',
	outlineColor: 'ink',
	outlineOffset: 'tiny',
};
